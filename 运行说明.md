# SinoCraft Vis 桌面版运行说明

## 可执行文件位置

当前已打包完成的桌面应用入口文件位于：

```text
release/win-unpacked/SinoCraft Vis.exe
```

在 Windows 操作系统中，直接双击该文件即可运行。

## 运行方式

1. 打开项目目录。
2. 进入 `release/win-unpacked/` 文件夹。
3. 双击 `SinoCraft Vis.exe`。


## 重要提示

不要只单独复制 `SinoCraft Vis.exe`。

该程序运行时还需要同目录下的 `resources/`、`locales/`、`.dll`、`.pak` 等文件。请保持整个 `win-unpacked` 文件夹结构完整，否则应用可能无法启动或显示空白页面。

如果需要发送给其他电脑使用，请压缩并发送整个文件夹：

```text
release/win-unpacked/
```

对方解压后，双击其中的 `SinoCraft Vis.exe` 即可运行。

## 创建桌面快捷方式

如果希望从桌面启动：

1. 右键点击 `SinoCraft Vis.exe`。
2. 选择“发送到”。
3. 选择“桌面快捷方式”。

之后可以通过桌面快捷方式直接打开应用。

## 安全提示

如果 Windows 出现安全提示或 SmartScreen 提醒，这是因为当前应用没有进行代码签名。

可以按以下方式继续运行：

1. 点击“更多信息”。
2. 点击“仍要运行”。

## 常见问题

如果双击后没有反应，建议检查：

- 是否从完整的 `win-unpacked` 文件夹中运行。
- `resources/app.asar` 是否存在。
- 是否被杀毒软件或系统安全策略拦截。
- 路径是否过长；如有异常，可以把整个 `win-unpacked` 文件夹复制到较短路径，例如 `D:\SinoCraftVis\` 后再运行。

该桌面版已经内置运行环境，普通用户不需要安装 Node.js、npm 或 Electron。
